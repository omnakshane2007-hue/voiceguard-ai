# VOICEGUARD AI — Production Requirements
# Python 3.10+
# Generated for Railway persistent server deployment

# ── Web Framework & Server ──────────────────────────────────────────
flask>=3.0.0
gunicorn>=21.0.0
flask-cors>=4.0.0

# ── AI / ML ─────────────────────────────────────────────────────────
# PyTorch CPU build (Railway Linux) — installs torch without CUDA overhead
torch==2.3.1+cpu --index-url https://download.pytorch.org/whl/cpu

# ── Audio Processing ─────────────────────────────────────────────────
librosa>=0.10.0
scipy>=1.11.0
soundfile>=0.12.0
numpy>=1.24.0

# ── Gemini API ────────────────────────────────────────────────────────
google-genai>=1.0.0

# ── Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
